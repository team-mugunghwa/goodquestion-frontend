# 백엔드 요청 — 말하기 후 활동 카드 이미지 (2026-08-15)

대상 저장소: `team-mugunghwa/goodquestion-backend` (`develop`)
요청 배경: 말하기 후 활동(순서 맞추기 → 다시 말하기) 화면을 구현하면서 [API.md](API.md) 2.10 의
DTO 계약과 확정된 활동 방식을 대조했다. 카드가 **텍스트 전제**로 설계돼 있어 그림 카드로는
화면을 그릴 수 없다.

기획 확정안: 주요 장면 카드 4~5개를 무작위 제시 → 아이가 이야기 순서대로 배열 →
정답이면 핵심 단어 3~4개 제공 → 카드와 단어를 참고해 전체 이야기를 음성으로 재구성

| # | 구분 | 내용 | 우선순위 |
|---|---|---|---|
| 1 | 계약 | `PostActivityStartResponse.Card` 에 `imageUrl` 추가 | 높음 |
| 2 | 콘텐츠 | 방귀 뀌는 며느리 카드 4장 · 장면당 낱말 1개 시드 | 높음 |
| 3 | 계약 | `retellingKeywords` 를 정답 순서와 같은 순서로 (장면과 1:1) | 높음 |
| 4 | 전제 | 카드 개수는 이야기마다 다르다 (고정 아님) | — |

---

## 1. `Card` 에 `imageUrl` 추가

**대상**: `PostActivityStartResponse.Card` (`PostActivityStatusResponse` 가 재사용)

### 현재

```
Card: cardId · text
```

### 요청

```
Card: cardId · text · imageUrl
```

**활동이 "그림만 보고 순서 맞추기"로 확정됐다.** 카드에 장면 제목을 띄우면 정답이 그대로
새어 나가므로 화면에는 그림만 그린다. `text` 만으로는 카드를 그릴 수 없다.

> `Card` 에 `keyword` 를 넣지 **말아 달라.** 낱말을 장면에 붙여 그리기로 했지만(3장),
> `Card` 는 1단계 시작 시점에 내려오므로 낱말이 붙어 있으면 **순서 정답이 새어 나간다.**
> 낱말은 지금처럼 `retellingKeywords` 로, 순서를 맞춘 뒤에만 내려주는 게 맞다.
> 대신 4장의 순서 보장이 필요하다.

`imageUrl` 은 다른 이미지 필드(`storyImage` · `coverImage` · `SceneContentResponse.imageUrl`)와
똑같이 **서버 정적 서빙 경로**(`/stories/**`)로 내려주면 된다.

> 대화 장면 캐릭터 에셋은 프런트 번들로 갔지만([BACKEND_REQUESTS.md](BACKEND_REQUESTS.md) 부록)
> 카드는 서버 URL 로 간다. 캐릭터는 상태 머신이 골라 쓰는 **런타임 에셋**이라 번들이 맞았고,
> 카드는 이야기가 늘어날 때마다 늘어나는 **콘텐츠**라 앱 배포 없이 추가돼야 하기 때문이다.

### `text` 는 남겨 달라

화면에 그리지는 않지만 **스크린리더 라벨로 쓴다.** 그림만 있는 카드는 라벨이 없으면
활동 자체가 불가능하다. 아래 시드 표의 문장 정도면 충분하다.

---

## 2. 시드 — 방귀 뀌는 며느리 (`story_11`)

### 카드 4장 — 아래가 정답 순서

| 순서 | 장면 | `text` (스크린리더 라벨) |
|---|---|---|
| 1 | 며느리가 방귀를 참아 얼굴이 노래짐 | 며느리가 방귀를 참느라 시무룩하게 서 있어요 |
| 2 | 시아버지의 갓이 날아가고 며느리가 쫓겨남 | 며느리의 방귀에 시아버지의 갓이 날아가 시아버지가 화를 냈어요 |
| 3 | 방귀로 배나무의 배를 떨어뜨림 | 며느리가 방귀로 배나무의 배를 우수수 떨어뜨렸어요 |
| 4 | 마을 사람들이 배를 얻고 고마워함 | 마을 사람들이 배를 얻고 며느리에게 고마워했어요 |

### 핵심 단어 — 장면당 하나

| 순서 | 장면 | `keyword` |
|---|---|---|
| 1 | 며느리가 방귀를 참아 얼굴이 노래짐 | `참다` |
| 2 | 시아버지의 갓이 날아가고 며느리가 쫓겨남 | `쫓겨나다` |
| 3 | 방귀로 배나무의 배를 떨어뜨림 | `떨어뜨리다` |
| 4 | 마을 사람들이 배를 얻고 고마워함 | `자신감` |

**낱말을 장면 그림에 붙여 그린다.** 낱말만 따로 칩으로 모아 두면 아이에게 "참고자료"로
읽혀서 안 쓰고 넘어간다. 장면 아래에 붙어 있어야 "이 장면을 말할 땐 이 말을 쓰는 것"이
된다. 이 활동의 목표가 어휘 사용이므로 붙이는 쪽이 맞다.

1단계(순서 맞추기)에서는 **낱말을 보여주지 않는다.** 낱말이 곧 장면의 정답 단서라
순서가 새어 나간다. 2단계는 이미 정답 순서가 공개된 뒤라 붙여도 문제없다.

### 이미지 원본 전달

프런트가 갖고 있다 — 1920x1080 PNG 4장. **전달 방식을 알려 달라** (백엔드 저장소 커밋 /
스토리지 직접 업로드 / 파일 전달 중 무엇인지).

---

## 3. 요청 — `retellingKeywords` 의 순서를 보장해 달라

**대상**: `CardSubmitResponse.retellingKeywords` · `PostActivityStatusResponse.retellingKeywords`

낱말을 장면 그림에 붙여 그리므로, 프런트는 **`retellingKeywords[i]` 를 정답 순서 i 번째
장면의 낱말로 짝짓는다.** 이 대응이 응답에서 보장돼야 한다.

```
retellingKeywords = ["참다", "쫓겨나다", "떨어뜨리다", "자신감"]
                       └ 장면1    └ 장면2      └ 장면3       └ 장면4
```

지금 명세에는 순서에 대한 언급이 없다. **정답 순서와 같은 순서로 내려준다**고 명시해 달라.
낱말 개수도 장면 개수와 같아야 한다 — 지금 명세의 "3~4개"와 카드 "4~5개"가 어긋나 있다.

개수가 어긋나도 화면은 깨지지 않지만, 둘 다 정상 상태는 아니다.

- 낱말이 **적으면**: 남는 장면을 낱말 없이 그린다(그 카드는 그림이 낱말 자리까지 쓴다).
- 낱말이 **많으면**: 남는 낱말을 마지막 장면 카드에 몰아 붙인다(`자신감 · 용기`).
  버리지 않는 이유는 어휘가 화면에서 사라진 걸 아무도 모르게 되기 때문이고, 새 줄을
  만들지 않는 이유는 이번에 없앤 "참고자료 칩 줄"이 그대로 돌아오기 때문이다.

---

## 4. 전제 — 카드 개수는 고정이 아니다

**이야기마다 카드 개수가 다르다.** 기획 원문의 "4~5개"는 예시 범위이지 상한이 아니다.

그래서 프런트는 개수를 고정하지 않고 **응답 길이대로 슬롯을 그린다** — 5장이 오면 슬롯도
5칸이 된다. 백엔드도 카드 개수에 대한 검증을 4로 박지 말아 달라.

방귀 뀌는 며느리가 4장인 것은 이 이야기의 값일 뿐이다. 아래 시드를 보고 "카드는 4장"으로
읽지 않도록 주의.

---

## 부록. 서버가 열릴 때까지의 프런트 구현

2.10 의 네 엔드포인트가 모두 501 이라, 그동안 프런트는 화면 코드에 하드코딩한다.

| 지금 | 서버가 열리면 |
|---|---|
| 카드 4장이 화면 코드에 상수로 박혀 있음 | `POST /post-activity/start` 응답 |
| 정답 순서를 프런트가 알고 로컬 판정 | `POST /post-activity/order` — **판정은 서버만 한다** |
| 키워드 4개가 상수 | `CardSubmitResponse.retellingKeywords` |
| 발화 텍스트가 데모 문자열 | `POST /post-activity/retelling` |

위 네 줄만 갈아 끼우면 된다. **계약은 그대로다** — 아래 5장처럼 화면이 바뀐 뒤에도
`/retelling` 은 여전히 `text` 하나만 받는다.

로컬 폴백 에셋: `assets/images/recap/banggui/scene_{1,2,3,4}.webp` (960x540 WebP, 총 400KB).
`imageUrl` 이 오기 시작하면 이 에셋은 네트워크 실패 시 폴백으로만 남긴다.

---

## 5. 2026-08-20 — 다시 말하기가 장면별로 바뀌었다 (계약 변경 없음)

2단계가 **"장면 4장을 늘어놓고 이야기를 통째로 한 번 녹음"에서 "장면 하나씩 묻고 답하는
대화"로** 바뀌었다. 백엔드가 할 일은 없지만, 요청 패턴이 달라져서 남긴다.

| | 전 | 후 |
|---|---|---|
| 녹음 | 이야기 전체를 한 번 (최대 60초) | 장면마다 한 번 |
| `/api/stt` | 활동당 1회 | 활동당 **장면 수만큼**(지금 4회) |
| `/retelling` | 1회 | **1회 (그대로)** — 프런트가 장면별 발화를 이어 붙여 보낸다 |
| `text` | 발화 하나 | 장면별 발화를 순서대로 공백으로 이어 붙인 것 |
| `sttRawText` | 원문 하나 | 같은 방식으로 이어 붙인 것. 원문이 하나도 없으면 `null` |

건너뛴 장면은 이어 붙이기에서 **빠진다** — 마이크가 계속 실패하는 아이가 활동에 갇히지
않도록 두 번 연속 실패하면 건너뛸 수 있게 했기 때문이다. 그래서 `text` 가 이야기의 일부만
담고 올라올 수 있다. 답이 하나도 없으면 아예 제출하지 않는다.

**`/api/stt` 호출이 활동당 4~5회로 늘어난 것만 확인해 달라.** 레이트리밋에 걸리면
`AI_RATE_LIMITED` 가 장면 중간에 뜨는데, 화면은 그 자리에서 재녹음 안내로 처리하지만
아이가 같은 장면에서 계속 막히게 된다.

### 낱말 사용 체크는 프런트가 한다

아이가 그 장면의 낱말을 실제로 썼는지 보고 체크 표시를 켠다. 한글 어간 매칭을 프런트에서
하므로(`lib/core/text/korean_keyword_match.dart`) **서버가 판정할 것은 없다.** 다만 불규칙
용언(듣다→들어, 굽다→구워)은 규칙으로 못 잡아서, 낱말별 활용형 목록을 내려줄 수 있으면
그대로 꽂을 자리는 만들어 뒀다. 지금 시드의 네 낱말은 규칙으로 다 잡히므로 **당장 필요하지
않다.**
